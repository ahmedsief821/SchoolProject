#include <stdio.h>

int main() {
    int x = 5;
    if (x == 0) {
        printf("Zero\n");
    } else if (x > 0) {
        printf("Positive\n");
    } else {
        printf("Negative\n");
    }
    return 0;
}
