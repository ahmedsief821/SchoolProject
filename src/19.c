#include <stdio.h>
#include <stdlib.h>

int main() {
    int array[5]; // Array to store some integer values

    printf("Enter 5 integers:\n");
    for (int i = 0; i < 5; ++i) {
        scanf("%d", &array[i]);
    }

    // Perform some operations on the array
    // Example: Sort the array, find the maximum value
    // ...

    return 0;
}
